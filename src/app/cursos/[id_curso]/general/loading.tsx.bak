import CursoForm from './cursoForm';
import Coordinaciones from './coordinaciones';
import Encargados from './encargados';
import { CursoType } from '@models/curso';
import { CoordinacionCursoType, CoordinacionType } from '@/models/coordinacion';
import { EncargadoType } from '@/models/encargado';
import { MaestroType } from '@/models/maestro';
import { RolType } from '@/models/rol';


const LoadingGeneral = () => {
    const loadingCurso: CursoType = {
        id: 0,
        clave: '',
        nombre: 'Loading...',
        nombre_ingles: null,
        horas_teoricas: null,
        horas_practicas: null,
        horas_independientes: null,
        horas_semana: null,
        horas_semestre: null,
        vinculo_objetivos_posgrado: null,
        id_rol:0,
    };
    const token: string = '';
    const idCurso: number = 0;

    const loadingCatalogoCoordinaciones: CoordinacionType[] = [];
    const loadingCoordinaciones: CoordinacionCursoType[] = [];
    const loadingEncargados: EncargadoType[] = [];
    const loadingCatalogoMaestros: MaestroType[] = [];
    const loadingCatalogoRoles:RolType[] = [];

    return (
        <section className='flex flex-col h-full p-2'>
            <CursoForm
                className='p-2 border border-light rounded'
                curso={loadingCurso}
                token={token}
                isLoading={true}
            />
            <div className='flex h-full'>
            <Coordinaciones
                idCurso={idCurso}
                coordinaciones={loadingCoordinaciones}
                catalogo_coordinaciones={loadingCatalogoCoordinaciones}
                token={token}
                className='p-2 w-1/2 h-full border border-light rounded mt-2 mr-1'
            />
            <Encargados
                idCurso={idCurso}
                encargados={loadingEncargados}
                catalogoMaestros={loadingCatalogoMaestros}
                catalogoRoles={loadingCatalogoRoles}
                className='p-2 w-1/2 h-full border border-light rounded mt-2 ml-1'
                token={token}
            />
            </div>
        </section>
    );
};

export default LoadingGeneral;
