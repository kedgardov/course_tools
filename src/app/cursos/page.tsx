
const ListadoCursos = ({ className }:{ className: string }) => {

    return (
        <div className = {`${className}`}>
            <p>Mis Cursos</p>
        </div>
    );
};

export default ListadoCursos;
